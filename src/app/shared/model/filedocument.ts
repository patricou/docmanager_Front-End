export class FileDocument {

    constructor(
        id: string,
        idInGrid: string,
        filename: string,
        contentType: string,
        createdDate: Date,
        owner: string,
        word: string,
        size: string
    ) {
    }
}